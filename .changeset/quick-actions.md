---
"battlify": minor
---

Add **Quick Actions** to the menu: **Dim/Brighten** the display (private
DisplayServices brightness), **Display Off** (`pmset displaysleepnow`), and
**Sleep** (`pmset sleepnow`). Fan control was intentionally not added — Apple
Silicon locks SMC fan writes and forcing the fan off risks overheating.
