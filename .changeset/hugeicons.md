---
"battlify": minor
---

Refresh the UI with **HugeIcons** for a more premium look. Added a lightweight
HugeIcons renderer (real stroke icons from `@hugeicons/core-free-icons`, drawn as
SwiftUI shapes — no runtime dependency) and migrated the menu popover to it: the
header battery, section icons, charge/pause states, Quick Actions, and footer.
